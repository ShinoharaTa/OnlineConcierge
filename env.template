# =============================================================================
# 篠川 Nostr Bot 環境設定ファイル テンプレート
# =============================================================================
# このファイルをコピーして .env として保存し、必要な値を設定してください

# =============================================================================
# 基本設定 (必須)
# =============================================================================

# Bot用のNostr秘密鍵 (HEX形式・64文字)
# この秘密鍵でBotがNostrネットワークに接続します
HEX=your_bot_private_key_64_characters

# =============================================================================
# Bot機能拡張設定 (オプション)
# =============================================================================

# おじさんBot用の秘密鍵 (HEX形式・64文字)
# 設定するとおじさん構文でランダム返信する機能が有効になります
OJI_HEX=your_ojisan_bot_private_key

# パスポートBot用の秘密鍵 (HEX形式・64文字)
# 定期的なパスポートメッセージ送信機能用
PASSPORT_HEX=your_passport_bot_private_key

# パスポート送信先のnpub (省略可)
# 未設定の場合はデフォルト値を使用
PASSPORT_TARGET_NPUB=npub1...

# =============================================================================
# LLM連携設定 (推奨・CalendarBot高精度化)
# =============================================================================

# OpenRouter API キー (推奨)
# 複数のLLMプロバイダーにアクセス可能、コスト効率良い
OPENROUTER_API_KEY=sk-or-v1-...

# 使用するLLMモデル名 (OpenRouter/OpenAI)
# 推奨: anthropic/claude-3.5-sonnet (日本語に強い)
LLM_MODEL_NAME=anthropic/claude-3.5-sonnet

# OpenAI API キー (フォールバック)
# OpenRouterが使用できない場合の代替
OPENAI_API_KEY=sk-...

# =============================================================================
# Discord監視通知機能 (オプション)
# =============================================================================

# Discord Webhook URL (MonitorBot有効化に必須)
# Discord サーバー設定 → 連携サービス → ウェブフック で作成
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# 監視対象キーワード (カンマ区切り)
# これらの単語を含む投稿をDiscordに通知
MONITOR_KEYWORDS=緊急,スパム,注意,アラート,重要

# 監視対象npub - 投稿元 (カンマ区切り)
# これらのユーザーからの投稿をDiscordに通知
MONITOR_NPUBS=npub1...,npub2...,npub3...

# 監視対象npub - メンション宛先 (カンマ区切り)
# これらのユーザーへのメンション（言及）をDiscordに通知
MONITOR_MENTION_NPUBS=npub1your_own_npub...,npub1friend_npub...

# =============================================================================
# InfluxDB連携設定 (MyRoomBot用・オプション)
# =============================================================================

# InfluxDB接続URL
# 例: http://localhost:8086 または https://your-influxdb-cloud.com
INFLUXDB_URL=http://localhost:8086

# InfluxDBアクセストークン
# InfluxDB管理画面で生成したAPIトークン
INFLUXDB_TOKEN=your_influxdb_token

# InfluxDB組織名
# InfluxDBで設定した組織（Organization）名
INFLUXDB_ORG=my-org

# InfluxDBバケット名
# センサーデータが保存されているバケット名
INFLUXDB_BUCKET=home-temp

# =============================================================================
# 開発・テスト設定
# =============================================================================

# テストモード (true/false)
# trueの場合、実際の投稿は行われずコンソール出力のみ
TEST_MODE=false

# =============================================================================
# 設定例・推奨構成
# =============================================================================

# 最小構成 (基本機能のみ):
# HEX=your_bot_private_key

# 推奨構成 (高機能):
# HEX=your_bot_private_key
# OPENROUTER_API_KEY=sk-or-v1-...
# LLM_MODEL_NAME=anthropic/claude-3.5-sonnet
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# MONITOR_KEYWORDS=緊急,重要,注意
# MONITOR_MENTION_NPUBS=npub1your_own_npub...

# フル構成 (全機能有効):
# 上記 + OJI_HEX, PASSPORT_HEX, MONITOR_NPUBS, MONITOR_MENTION_NPUBS
# + INFLUXDB_URL, INFLUXDB_TOKEN, INFLUXDB_ORG, INFLUXDB_BUCKET 等 